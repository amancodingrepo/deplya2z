# Flutter does not load this file automatically.
# Pass this value at build time:
# flutter build apk --release --dart-define=API_BASE_URL=https://api.example.com/v1
#
# The current HTTP/IP backend can work only because Android cleartext traffic is
# explicitly enabled in android/app/src/main/AndroidManifest.xml.
# Prefer HTTPS before distributing production builds.
API_BASE_URL=https://api.example.com/v1
